# Pinned dependencies for local development and CPU smoke tests.
#
# Production experiments were run on RunPod RTX 5090 pods (CUDA 12.8,
# sm_120), which requires torch>=2.7 with the cu128 wheel index:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
# For CPU-only or macOS, drop the +cu121 suffix below and install the
# plain torch==2.5.1 / torchvision==0.20.1 build instead.

# Deep learning / core
torch==2.5.1+cu121
torchvision==0.20.1+cu121

# Numerical / data handling
numpy==1.26.4
pandas==2.1.4
scipy==1.15.2
tqdm==4.67.1

# Machine learning / metrics
scikit-learn==1.6.1
scikit-image==0.25.2

# Geospatial / raster I/O
rasterio==1.4.3

# Visualization
matplotlib==3.10.0
pillow==11.1.0

# Experiment tracking
mlflow==2.9.2

# Data acquisition (Google Earth Engine): optional
earthengine-api==1.5.1
geemap==0.35.1
requests==2.32.3
python-dotenv==1.0.1

# Development / testing
pytest==9.0.3
pytest-mock==3.15.1
ruff==0.15.12
